#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/Users/imenkedir/dev/web/Spect-AI/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="$NODE_PATH:/Users/imenkedir/dev/web/Spect-AI/node_modules/.pnpm/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../../next@13.2.4_react-dom@18.2.0_react@18.2.0/node_modules/next/dist/bin/next" "$@"
else
  exec node  "$basedir/../../../../../../next@13.2.4_react-dom@18.2.0_react@18.2.0/node_modules/next/dist/bin/next" "$@"
fi
